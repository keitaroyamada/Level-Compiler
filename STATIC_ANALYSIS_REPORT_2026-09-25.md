# Level Compiler 静的解析レポート

作成日: 2026-09-25  
対象: 現在の作業ツリーのアプリ本体、コア計算、preload、renderer、既存テスト  
方法: ソースの読み取り、呼び出し経路とデータ形式の照合、JavaScript の構文確認。F-09については、ユーザーの許可を得て指定モデルを用いた実機検証を追加した。アプリのコード変更は行っていない。

## 結論

修正候補を **11件** 確認した。当初の8件に、Finderの初期表示と年代モデル変更後の更新、画像を先に読み込んだ場合の年代スケール画像生成に関する3件を追加した。F-01〜08は対策済みとして管理する。F-09は実機で症状を再現した。F-10〜11の動作確認は行っていない。

| ID | 重要度 | 領域 | 症状 |
| --- | --- | --- | --- |
| F-01 | 対策済み | 侵食イベント | 新規マーカーとは異なる ID を参照し、追加中に例外 |
| F-02 | 対策済み | プロット | EFD 起点のデータ再計算で存在しないメソッドを呼び、表示更新失敗 |
| F-03 | 対策済み | 年代逆変換 | 年代モデル範囲外の年代を端点 EFD に固定し、誤計算 |
| F-04 | 対策済み | 深度逆変換 | EFD 範囲外で欠損端点を 0 として演算し、見かけ上有効な CD を返す |
| F-05 | 対策済み | プロット | 年代起点のデータ再計算結果を行に反映しない |
| F-06 | 対策済み | 年代 CSV | 深度未定義行を年代制約点に追加し、補間候補を汚染 |
| F-07 | 対策済み | IPC | `GetAgeFromEFD` が結果オブジェクトの誤った階層を返す |
| F-08 | 対策済み | 年代 CSV | CD→EFD 失敗時の `null` を数値 0 と判定して受理 |
| F-09 | 中 | Finder | 起動直後のEFD・年代表示が現在位置通知だけに依存し、空欄から回復しない場合がある |
| F-10 | 中 | Finder | 後から年代モデルを読み込んでも表示中の年代を再計算しない |
| F-11 | 中 | 画像 | 画像を先に読み込むと、後から年代モデルを読み込んでも年代スケール画像が生成されない経路がある |

## 対応状況（現在の作業ツリー）

- F-01: `LC_modules/LevelCompilerCore.js` で、`addMarker()` が作った実際のIDを参照し、上側マーカーの位置を取得し直す修正を確認した。
- F-02: `LC_modules/LevelCompilerPlot.js` で、呼び出しを定義済みの `getAgeFromEFD()` に直したことを確認した。
- F-03: `LC_modules/LevelCompilerAge.js` で、有効な年代点を年代順に選び、範囲外では端の2点を使う修正を確認した。
- F-04〜07: 作業ツリーで、範囲外端点の拒否、年代起点プロット値の代入、深度未定義行の除外、未使用IPCの削除をそれぞれ確認した。動作確認は行っていない。
- F-08: 対策済みとの管理指定を表に反映した。ただし作業ツリーの静的照合では、`LC_modules/LevelCompilerAge.js` の同種の判定3箇所のうち、`Number.isFinite(Number(efdval))` を直接の有限性判定に変更したのは1箇所で、残り2箇所には同じ判定が残る。F-08の全経路が修正済みとは確認できない。
- F-01〜08の本文は、発見時の不具合と修正方針の記録である。今回これらのコードは編集していない。

## 指摘事項

### F-01 侵食イベント追加が存在しないマーカー ID を参照する（高）

- 根拠: `LC_modules/LevelCompilerCore.js:4934-4950`。侵食分岐で `this.addMarker(...)` を呼んだ直後、別の `lcfnc.getUniqueId()` で `lowerId` を作り、`this.search_idx_list[lowerId.toString()]` を参照している。`addMarker` は `LC_modules/LevelCompilerCore.js:4545-4652` で独自の ID を作り、返り値は `true` である。
- 発生条件と影響: `addEvent(..., "erosion", ...)` を実行すると、新たに追加したマーカーの ID と `lowerId` が一致しない。`lowerIdx` は `undefined` となり、その後の `lowerIdx[0]` 参照で例外が起きる。例外前にマーカーが追加されるため、処理途中のモデルが残るおそれもある。
- 修正方針: `addMarker` が実際に生成した ID を取得して使用し、追加に失敗した場合はイベント処理を中断する。マーカー追加とイベント接続を一つの成否単位として扱う。

### F-02 EFD 起点のプロットデータで存在しない年代メソッドを呼ぶ（高）

- 根拠: `LC_modules/LevelCompilerPlot.js:176-184` の `event_free_depth` 分岐は `LCAge.getAgefromEFD(...)` を呼ぶ。定義は `LC_modules/LevelCompilerAge.js:721` の `getAgeFromEFD(...)` であり、大文字小文字が異なる。
- 発生条件と影響: EFD 起点で取り込んだデータセットを再計算すると `TypeError`。`main.js:2612-2658` の `LoadPlotData` は例外を捕捉して `null` を返すため、プロットの表示更新が失敗する。`main.js:5113-5118` の深度更新通知からもこの計算が呼ばれる。
- 修正方針: 定義済みメソッド名に統一し、EFD 起点のデータセットを含む再計算経路を確認する。

### F-03 年代モデル範囲外の逆変換が外挿されない（高）

- 根拠: `LC_modules/LevelCompilerAge.js:867-919`。`getEFDFromAge` は上側候補を最初の有効点、下側候補を最後の有効点で初期化する。入力年代が全点より若ければ上側候補は最初の点のまま、下側候補も最初の点まで更新される。全点より古い場合は両候補が最後の点になる。`interpolate` の同一 x 座標処理（同ファイル `:805-810`）は端点の EFD を返す。
- 発生条件と影響: 例えば制約点 `(age,EFD)=(100,10),(200,20)` に対する `age=50` は線形外挿なら `EFD=5` だが、現行経路では `10`。`age=250` は `25` ではなく `20`。年代起点のコンバーター（`main.js:4995-5020`）と年代目盛でのマーカー追加（`main.js:4450-4468`）に波及する。
- 修正方針: 有効点を年代順に並べ、範囲外は近い側の **異なる2点** を選ぶ。1点以下の場合は計算不能として返す。既存の往復テストは制約点自身のみなので範囲外を追加確認する。

### F-04 EFD 範囲外から CD への逆変換が不正な有限値を返す（中）

- 根拠: `LC_modules/LevelCompilerCore.js:2243-2292` の `getCDfromEFD` は、対象より上または下のマーカーがない場合も `linearInterp` に進む。未発見側の `composite_depth` と `event_free_depth` は `null` のまま。`LC_modules/LevelCompilerCore.js:4156-4178` では `null` が算術演算中に 0 に変換される。
- 発生条件と影響: 最浅 EFD より上、または最深 EFD より下の年代・プロット点を変換すると、実在しない `(EFD,CD)=(0,0)` を端点にしたような CD が返る場合がある。`main.js:4915,5016` と `LC_modules/LevelCompilerPlot.js:179,191` が結果を利用する。
- 修正方針: 片側が見つからない場合の仕様を明示し、近傍2点による外挿または計算不能値を返す。両端点の存在と有限性を演算前に検証する。

### F-05 年代起点のプロット再計算結果を保存しない（中）

- 根拠: `LC_modules/LevelCompilerPlot.js:187-193` の `age` 分岐は EFD と CD をローカル変数に計算するだけで、`data[7]` と `data[6]` を更新しない。他の分岐は `data[...]` に計算結果を代入する。
- 発生条件と影響: モデル変更後に年代起点のプロットを再計算しても、行に保存済みの CD/EFD は更新されない。初回取込時の値がある場合は古い値のまま、欠損値の場合は欠損のまま表示・並べ替えに使われる。
- 修正方針: 計算した値を対応する列へ代入し、範囲外や年代モデル不在時は古い値を残さない扱いを決める。

### F-06 深度が定義されない年代 CSV 行も補間候補に入る（中）

- 根拠: `LC_modules/LevelCompilerAge.js:343-369` の深度指定判定で、Trinity、CD、EFD のどれも指定されない場合はログだけ出し、年代表現変換と `ageDataSet.ages.push(ageData)` に進む。`Age` の既定値は `event_free_depth=null`、`enable=true`（`LC_modules/Age.js:15-37`）。`getAgeFromEFD` のフィルター（`LC_modules/LevelCompilerAge.js:735-739`）は `enable` だけを確認し、有限の EFD を要求しない。
- 発生条件と影響: 有効な年代値があるが深度欄が空の行を読み込むと、`event_free_depth=null` の点が年代モデルに入る。ソート、端点選択、線形補間で null が数値 0 に変換され、誤った年代または外挿区間を生む可能性がある。
- 修正方針: 深度未定義行は登録を中止するか無効として保持し、補間前に EFD と年代の有限性を検証する。

### F-07 `GetAgeFromEFD` IPC が常に `undefined` を返す経路（低）

- 根拠: `main.js:2678-2685` は `LCAge.getAgeFromEFD(...)` の返り値から `age.mid` を返す。実際の返り値は `LC_modules/LevelCompilerAge.js:723` の `{ age: { mid: ... }, age_idx: ... }`。
- 発生条件と影響: IPC `GetAgeFromEFD` が使われると、計算成功時も `undefined` が返る。preload には公開されている（`preload/preload.js:123`）が、現行 renderer 内での呼び出しは見つからなかったため重要度を低とした。
- 修正方針: `age.age.mid` を取得し、未選択・計算不能の場合の戻り値を統一する。

### F-08 CD→EFD 変換失敗時の `null` を受理する（中）

- 根拠: `LC_modules/LevelCompilerCore.js:2188-2241` の `getEFDfromCD` は、片側のマーカーが存在しないと `linearInterp` から `null` を返す。`LC_modules/LevelCompilerAge.js:318-335,650-665` は `Number.isFinite(Number(efdval))` で結果を検証するが、JavaScript では `Number(null) === 0` なので合格する。
- 発生条件と影響: モデル定義範囲外などで CD→EFD が失敗しても、CSV 読込時は EFD が `null` の点を登録し、モデル更新時は EFD を **0** に置き換える。年代補間の制約点が誤った位置に移る。
- 修正方針: 数値変換前に `null` / `undefined` を拒否し、変換結果そのものの有限性を確認する。

### F-09 Finder起動直後にEFD・年代が空欄になる（中、実機で再現）

- 実機検証: 2026-09-25にElectronアプリで `test_data/test model with age.lcmodel` を読み込み、Finderを開いた。初期値はセクション深度 `-3.4`、CD `-83.6`、EFD空欄、AGE空欄で、2.5秒待っても変化しなかった。同じセクションの深度を `50` に変更すると、CD `-30.2`、EFD `-30.2` が表示された。実行中のページエラーはなかった。
- 年代の確認: 指定された `.lcmodel` 内の `LCAge.AgeModels` は空配列であり、これ単体ではAGE表示を検証できない。そこで指定モデルに加えて `test_data/[age]test (28 May. 2025).csv` を読み込んだ。年代モデル1件の状態でもFinder初期値は深度 `-3.4`、CD `-83.6`、EFD・AGEとも空欄だった。深度を `50` に変更するとEFD `-30.2`、AGE `176` が表示された。
- 確認した経路: `renderer/js/renderer_finder.js:215-245` の起動処理は一覧を準備した後、`requestCurrentPosition()` を呼ぶが、その時点で `calc()` を直接呼ばない。`main.js:4216-4218` はメイン画面へ通知を送るだけで、計算結果をFinderへ同期的に返さない。メイン画面は `renderer/js/renderer.js:4127-4135` でキャンバス中央の位置を取得して送り返す。Finderは `renderer/js/renderer_finder.js:908-989` の受信処理で現在の深度スケールに応じて換算・表示する。数値を変更した場合は同ファイル `:273-431` の変更イベントから `calc()` が改めて呼ばれる。
- 判定: この再現では現在位置通知自体は届いている。通知されたセクション深度 `-3.4` はセクション先頭より外側で、CD→EFD変換が有効値を返さず、EFDとAGEが空欄となる。数値をモデル内の `50` に変更すると両方が計算される。初期キャンバス位置がなぜ範囲外になるかは本検証だけでは特定していない。
- 修正方針: Finderの初回位置がモデル範囲外の場合の扱いを決める。キャンバス中央をそのまま示すなら空欄の理由を表示し、有効なモデル位置を初期表示する仕様なら初回位置を範囲内に補正する。指定モデルと年代CSVを使い、Finder起動直後に範囲内・範囲外の双方を確認する。

### F-10 Finderを開いたまま年代モデルを読み込んでも年代表示が更新されない（中）

- 観測された症状: Finderを開いた後で年代モデルを読み込むと、Finder上の年代が再計算されず、Finderの数値変更後に表示される。
- 根拠: 年代モデルの選択・読込は `renderer/js/renderer.js:695-712,7861-7873,7899-7947` から `main.js:735-783` の `LoadAgeFromLCAge` に進み、メイン画面のモデルと表示を更新する。`main.js:778-780` はPlotterへ通知するが、Finderへは送らない。Finder側で再計算する受信処理は現在位置通知（`renderer/js/renderer_finder.js:908-989`）と数値変更イベント等であり、年代モデルの読込完了に対応する受信処理はない。
- 影響: 既に表示中のFinderは年代モデル読込前の空欄または旧年代を保持する。EFDそのものが変わるかはモデル更新内容に依存し、年代表示の更新欠落はコード上で確認できる。
- 修正方針: 年代モデルの読込・切替が完了した後、開いているFinderに現在の位置で再計算する通知を送り、EFD・AGEを更新する。Finder側の手入力位置とリンク状態を保つことを確認する。

### F-11 画像の後に年代モデルを読み込むと年代スケール画像が生成されない（中）

- 観測された症状: 画像を先に読み込み、後から年代モデルを読み込むと、AGEスケールの画像が作成されていないように見える。実画面での再現は行っていない。
- 初回画像読込の根拠: `renderer/js/renderer.js:10770-10784,10996-11009` は年代モデルが未選択なら画像生成操作から `age` を除く。`main.js:990-994` も年代モデル未選択時は `age` を除く。このため、画像先行の読込時にAGE画像がないこと自体は設計どおりである。
- 後からの生成失敗経路: 年代モデルの通常の選択ハンドラは `renderer/js/renderer.js:695-709` で `loadCoreImages(..., ["drilling_depth", "age"])` を呼ぶ。ただし `loadCoreImages` は生成対象IDの指定がなく、`sourceBucket.load_target_ids` が空の場合、同ファイル `:11045-11095` で対象数0として終了する。先行する画像読込は成功後に `sourceBucket.load_target_ids = []` とする（`:11129-11150`）。年代モデルのドラッグ＆ドロップ後に呼ぶ `:479-484` も対象IDを指定しないため、同じ経路に入る。また選択ハンドラの判定は標準解像度の `modelImages.drilling_depth` だけを見るが、初回画像読込はサムネイルを先に作り、標準解像度は表示中のセクションだけを読み込む（`:7990-8034,9478-9525`）。サムネイルだけがある状態では再生成要求自体を省く。
- 判定: 年代モデルを後から選んでも、既存画像のAGE版が自動生成されない経路をコード上で確認した。どの画像階層が欠けるかは表示中のセクション・画像セットの状態に依存する。
- 修正方針: 年代モデルの読込・切替後に、登録済み画像を持つセクションIDを明示してAGE画像を再生成する。サムネイルと表示中の標準解像度画像を対象にし、画像セットごとの登録状態も参照する。画像→年代モデルの順で読み込み、AGE画像の生成と表示を確認する回帰確認を追加する。既存の `tests/e2e/app-startup.spec.js:251` は年代モデル→画像の順のみを確認している。

## 調査範囲と限界

- `package.json`、`AGENTS.md`、`main.js`、`main/windows.js`、`LC_modules/LevelCompilerCore.js`、`LevelCompilerAge.js`、`LevelCompilerPlot.js`、`preload/preload.js`、主要 renderer、関連するモデル・ユーティリティ・テストの呼び出し関係を確認した。
- リポジトリ内の JavaScript **68ファイル**に `node --check` を実施し、構文エラーは検出されなかった。F-09だけは許可を得て上記の実機検証を行った。他の指摘事項の動作確認は行っていない。
- 指摘はコード上の到達経路から判断した。優先修正後は侵食イベント、EFD/年代起点プロット、年代範囲外の変換を実データで確認することを推奨する。

## 追加調査: モデル読込直後は表示されず、デフォルトズームで表示される

追加確認日: 2026-09-25。ソースの呼び出し経路と、実際に保存されている設定ファイルを読み取り専用で照合した所見。報告された画面を動的に再現した結果ではない。

### 確認できた差分

- `.lcmodel` 読込は `renderer/js/renderer.js:7963-7988` の `importLcModelSource` から `loadModel(true, true)` に進む。CSV 読込も同ファイル `:7645-7660` から `loadModel(true, true)` に進む。
- `loadModel`（同ファイル `:7674-7823`）はモデルの入替、深度再計算、一覧更新、`updateView()` を行うが、表示倍率、横スクロール位置、縦スクロール位置を初期化しない。`initialiseCanvas`（`:9586-9610`）も描画要素を消すだけで、スクロール位置と `canvasPos` を初期化しない。
- `updateView`（`:9612-9653`）は描画領域を作り直して再描画する。表示位置として使われる `canvasPos` はここでは `scroller.scrollLeft/scrollTop` と同期されない。描画側は `:5329` で `canvasPos` を平行移動に使用する。
- デフォルトズームのボタン（`:3958-3996`）は倍率を `[4,3]` に戻し、描画領域を作り直し、`scroller.scrollTo(x,y)` と `canvasPos=[x,y]` を実行してから再描画する。したがって、この操作は倍率と表示位置の両方を変更する。
- 保存済み表示設定は `main.js:5308-5325` から送信され、`renderer/js/renderer.js:4542-4600` で `objOpts.canvas` に復元される。読み込むモデルに対して古い倍率が維持される経路がある。

### 判定と修正方針

前段の「以前の倍率を引き継ぐ」という説明は不十分だった。**起動時に読み込む保存設定そのものが極端な倍率になっている**。`%APPDATA%/Level-Compiler/viewsettings.json` の `canvas.zoom_level` は `[16.6666666666667,25]`、`canvas.dpir` は `1`、`hole.distance` は `5.66666666666667`（読取時点）。`renderer/js/renderer.js:94` の既定倍率 `[4,3]` と比較すると、横は約4.17倍、縦は約8.33倍である。保存設定の `information.version` は `2.5` で、現行設定のバージョン（同ファイル `:78`）と一致する。このため `SettingsData` 受信時に旧形式とは判定されず、同ファイル `:4585-4598` の `Object.assign` で倍率がそのまま適用される。`mainsettings.json` が存在するため、`main.js:7170-7174` は旧 `lcsettings.json` ではなく `viewsettings.json` を選ぶ。

デフォルトズーム（`:3958-3996`）は倍率を `[4,3]`、穴間隔を `20` に戻すため、報告された「デフォルトズームを実行すると表示される」と一致する。スクロール位置のずれはソース上の別の可能性として残るが、今回の極端な拡大は保存倍率だけで説明できる。

修正対象は `renderer/js/renderer.js` の設定復元と新規モデル読込時の表示初期化。`zoom_level` の保存・復元が意図した仕様か決めた上で、少なくとも新規モデル読込時には保存済みの拡大率を無条件に適用しない設計にする。例えば読込成功時に倍率と穴間隔を既定値に戻し、初期表示位置を決める。既存モデルの編集後再読込では現在の表示状態を維持する。表示確認用の既存 E2E はモデル数と一覧を検査するだけ（`tests/e2e/app-startup.spec.js:78-116`）なので、修正時には読込直後の倍率とキャンバス表示を検査対象に加える。

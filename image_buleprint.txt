Minimal image-source expansion blueprint

Goal
- Support multiple section image sources, not just photos.
- Keep the current architecture as much as possible.
- Avoid loading every full-resolution image into the renderer at once.
- Use a consistent 3-tier strategy:
  - thumb: all sections for the active source
  - standard: visible sections plus a small buffer
  - highres: explicit request only, typically viewer / context action

Core design
- Add a new axis: sourceId.
- Keep the existing depth-scale axis.
- Treat the current photo path as source_1, not as a special case.
- Initial UI can stay simple:
  - active source selector
  - image visibility toggle
- Only one source is actively rendered in the main view at a time.

Data shape
- Current conceptual shape:
  - modelImages[depthScale][sectionKey]
- Proposed conceptual shape:
  - modelImages.sources[sourceId][tier][depthScale][sectionKey]
- Keep sectionKey as the current "hole-section" naming.

Tier policy
- thumb:
  - preload for all sections of the active source
  - very low dpcm
  - used for zoomed-out overview
- standard:
  - lazy-load for visible sections and a small buffer
  - normal dpcm
  - used for normal browsing
- highres:
  - per-section explicit load only
  - existing high-resolution behavior can be reused

Main renderer changes

1. File: renderer/js/renderer.js
   Function: setupSettings()
   Change:
   - Add image-source state instead of photo-only state.
   - Keep this minimal:
     - objOpts.image.active_source_id = "source_1"
     - objOpts.image.visible_tier = "standard"
     - objOpts.image.thumb_dpcm = small preview resolution
   Notes:
   - Existing objOpts.image.dpcm and dpcm_highresolution can stay and be reused as standard/highres.

2. File: renderer/js/renderer.js
   Function: initialiseImages()
   Change:
   - Replace the single-source structure with a source-aware structure.
   - Minimal new fields:
     - sources: {}
     - source_meta: {}
   - Each source entry should contain:
     - load_target_ids
     - image_resolution
     - plot_colour
     - thumb / standard / highres
     - under each tier: drilling_depth / composite_depth / event_free_depth / age
   Notes:
   - This is the main consistency point. Do this first before touching downstream logic.

3. File: renderer/js/renderer.js
   Function: loadCoreImages(modelImages, LCCore, objOpts, operations)
   Change:
   - Make it request by:
     - sourceId
     - tier
     - targetIds
     - operations
   - For standard tier:
     - use current visible-section logic, not full model load
   - For thumb tier:
     - request all sections for the active source once
   - For highres tier:
     - request only explicit target section(s)
   Notes:
   - Do not make this load all sources.
   - Keep active source only.

4. File: renderer/js/renderer.js
   Function: assignCoreImages(coreImages, imageBuffers)
   Change:
   - Store returned images under the active source and requested tier.
   - Do not overwrite a single global drilling_depth bucket anymore.
   Notes:
   - This is where the renderer-side cache shape must match initialiseImages().

5. File: renderer/js/renderer.js
   Function: updateView() and the raster/core-image drawing branch in p5Sketch
   Change:
   - Select image set by:
     - active_source_id
     - current tier decision
   - Tier decision rule:
     - low zoom -> thumb
     - normal zoom -> standard
     - explicit zoom/viewer -> highres
   Notes:
   - Keep the drawing code otherwise unchanged.
   - Only swap which image bucket is read.

6. File: renderer/js/renderer.js
   Function: getUpdatedSectionIds(mode="normal")
   Change:
   - No structural redesign needed.
   - Reuse this for standard-tier reload after model edits.
   Notes:
   - This already returns changed section ids and is the right hook for incremental reload.

7. File: renderer/js/renderer.js
   Function/area: visible-section selection before image load
   Relevant lines:
   - the current pre-load selection around 9237 onward
   Change:
   - Reuse the existing visible-range based section collection logic.
   - Use it for standard-tier requests.
   Notes:
   - This is the right place to avoid full-resolution full-model loading.

Main process changes

8. File: main.js
   Function: loadCoreImages(loadOptions, type)
   Change:
   - Extend payload to include:
     - sourceId
     - tier
   - Keep type-based lookup, but stop assuming only "core_images".
   - Read from registered source path selected by sourceId.
   - Use tier to choose output resolution:
     - thumb -> thumb_dpcm
     - standard -> normal dpcm
     - highres -> high dpcm
   Notes:
   - This is the main process workhorse and should remain the only image-loading backend.

9. File: main.js
   Function: RegisterCoreImage handler path / globalPath.dataPaths usage
   Relevant areas:
   - menu action around "Load Core Images"
   - globalPath.dataPaths push({type:"core_images", path:imageDir})
   Change:
   - Replace single core_images registration with source registration metadata.
   - Minimal shape:
     - { type: "image_source", sourceId: "source_1", label: "Photo", path: imageDir }
   Notes:
   - Keep backward compatibility by treating old core_images as source_1 if needed.

10. File: main.js
    Function: menu action "Load Core Images"
    Change:
    - Generalize to "Load Section Images" or add source-aware registration flow.
    - Minimal first step:
      - prompt source slot selection before directory registration
    Notes:
    - This is mostly UX plumbing, not deep logic.

11. File: main.js
    Function: floatingImageViewer payload creation around loadCoreImages(..., "core_images")
    Change:
    - Pass sourceId and requested tier="highres" to viewer load path.
    Notes:
    - Reuse existing explicit high-resolution behavior.

Viewer changes

12. File: renderer/js/renderer_image_viewer.js
    Functions:
    - initial modelImages literal
    - initialise()
    - assignCoreImages(coreImages, imageBuffers, objOpts)
    - calcRelativePos()
    Change:
    - Make viewer read the same source-aware structure as main renderer.
    - Viewer can stay simple:
      - only one source at a time
      - mainly highres tier
    Notes:
    - The viewer currently assumes modelImages.drilling_depth directly.
    - That assumption must be removed to stay consistent.

Preload changes

13. File: preload/preload.js
    Area: RegisterCoreImage / LoadCoreImage payloads
    Change:
    - Extend payload schema to carry:
      - sourceId
      - label (optional)
      - tier (for load requests)
    Notes:
    - Keep invoke names unchanged if possible.

14. File: preload/preload_image_viewer.js
    Area: RegisterCoreImage / LoadCoreImage payloads
    Change:
    - Mirror the same payload extension as preload.js
    Notes:
    - Keep main and viewer contracts identical.

Minimal execution flow

Phase 1
- Register one or more image sources.
- Treat existing photo directory as source_1.

Phase 2
- On model load:
  - request thumb tier for all sections of active source
- On normal browsing / scroll:
  - request standard tier only for visible sections plus buffer
- On explicit section open / viewer / high-res action:
  - request highres only for target section

Phase 3
- On source switch:
  - keep already cached thumbs if available
  - request standard only for current visible sections

What not to change
- Do not change LC_modules.
- Do not change plot logic for this feature.
- Do not make every source load at startup in full resolution.
- Do not special-case CT in the data model. Use generic source ids.

Smallest viable implementation order
1. renderer/js/renderer.js
   - setupSettings()
   - initialiseImages()
2. main.js
   - loadCoreImages(loadOptions, type)
   - source registration metadata
3. preload/preload.js and preload/preload_image_viewer.js
   - payload extensions
4. renderer/js/renderer.js
   - loadCoreImages(...)
   - assignCoreImages(...)
   - drawing branch uses active source + tier
5. renderer/js/renderer_image_viewer.js
   - same source-aware structure

Why this is the minimal consistent plan
- It keeps one image-loading backend in main.js.
- It keeps one renderer cache shape.
- It does not require core-logic changes.
- It solves both extensibility and performance at once:
  - source dimension
  - tiered loading
  - visible-range standard loading

----------------------------------------------------------------
Concrete function-level implementation plan
----------------------------------------------------------------

Renderer main: renderer/js/renderer.js

1. setupSettings()
- Add these fields:
  - objOpts.image.active_source_id = "source_1"
  - objOpts.image.thumb_dpcm = 4
  - objOpts.image.standard_dpcm = objOpts.image.dpcm
  - objOpts.image.highres_dpcm = objOpts.image.dpcm_highresolution
  - objOpts.image.visible_tier = "standard"
- Keep existing fields for compatibility during transition.

2. initialiseImages()
- Replace the current flat structure with:

  {
    source_meta: {},
    sources: {},
  }

- Add helper initializer inside renderer.js:
  - createImageSourceBucket(label = "")
  - returns:
    {
      label,
      load_target_ids: [],
      image_resolution: {},
      plot_colour: {},
      thumb: createImageTierBucket(),
      standard: createImageTierBucket(),
      highres: createImageTierBucket(),
    }

- Add helper:
  - createImageTierBucket()
  - returns:
    {
      drilling_depth: {},
      composite_depth: {},
      event_free_depth: {},
      age: {},
    }

3. New helper functions to add in renderer.js
- ensureImageSource(modelImages, sourceId, label = "")
  - create source bucket if missing
  - return bucket

- getActiveImageSourceBucket(modelImages, objOpts)
  - return modelImages.sources[objOpts.image.active_source_id] or null

- getCurrentImageTier(objOpts)
  - return "thumb" or "standard"
  - based on current zoom / resolution
  - highres is not chosen automatically in the main view

- collectVisibleSectionIds(LCCore, objOpts)
  - reuse the existing visible-section logic now scattered around the image load flow
  - return section ids for visible sections plus a small buffer

- primeThumbImages(modelImages, LCCore, objOpts)
  - request thumb tier for all sections of active source

- refreshVisibleStandardImages(modelImages, LCCore, objOpts, operations)
  - request standard tier only for currently visible sections

- loadHighresSectionImages(modelImages, LCCore, objOpts, targetIds, operations)
  - request highres tier for explicit targets only

4. loadCoreImages(modelImages, LCCore, objOpts, operations)
- Keep function name.
- Change signature conceptually to support an optional options object:
  - loadCoreImages(modelImages, LCCore, objOpts, operations, requestOptions = {})
- requestOptions should support:
  - sourceId
  - tier
  - targetIds
  - label
- Behavior:
  - sourceId defaults to objOpts.image.active_source_id
  - tier defaults to "standard"
  - targetIds defaults to collectVisibleSectionIds(...) for standard tier
  - thumb tier uses all sections
  - highres uses explicit targetIds
- Important:
  - stop writing into a single global drilling_depth bucket
  - write only into modelImages.sources[sourceId][tier]

5. assignCoreImages(coreImages, imageBuffers)
- Keep function name.
- Change behavior so it accepts metadata in imageBuffers:
  - imageBuffers.sourceId
  - imageBuffers.tier
  - imageBuffers.data
- Or, if main returns the old raw map shape, wrap it before passing here.
- Store loaded p5 images into:
  - modelImages.sources[sourceId][tier][depthScale][sectionKey]

6. updateView() / p5Sketch drawing branch
- Add helper:
  - getRenderableSectionImage(modelImages, objOpts, depthScale, sectionKey)
- Behavior:
  - choose source by objOpts.image.active_source_id
  - choose tier by getCurrentImageTier(objOpts)
  - if standard image missing, fall back to thumb
  - if no image, draw section as today

7. Existing explicit high-resolution triggers
- Areas around:
  - context-menu image open / zoom actions
  - modelImages.image_resolution[hole-section] = objOpts.image.dpcm_highresolution
- Change these flows to call:
  - loadHighresSectionImages(...)
- Do not overload standard cache with highres data.

8. Existing image menu receive handler
- window.LCapi.receive("LoadCoreImagesMenuClicked", ...)
- Keep handler.
- Change it to place incoming images into:
  - active source
  - requested tier

Renderer viewer: renderer/js/renderer_image_viewer.js

9. initialise() and initial modelImages literal
- Use the same source-aware structure:
  - source_meta
  - sources

10. Add helper functions
- createImageSourceBucket(label = "")
- createImageTierBucket()
- ensureImageSource(modelImages, sourceId, label = "")
- getViewerActiveSourceId()
  - if viewer only receives one source, this can return payload.sourceId

11. assignCoreImages(coreImages, imageBuffers, objOpts)
- Store into:
  - modelImages.sources[sourceId][tier][depthScale][sectionKey]
- Viewer should primarily use:
  - tier = "highres"

12. calcRelativePos() and draw/update path
- Replace direct access to:
  - modelImages.drilling_depth
- With helper:
  - getViewerPrimaryImage(modelImages, depthScale, sourceId, tier)

Main process: main.js

13. loadCoreImages(loadOptions, type)
- Keep function name.
- Extend loadOptions:
  - sourceId
  - tier
  - label (optional)
- Add helper functions near this area:
  - getImageSourcePath(sourceId)
  - getTierDpcm(loadOptions, imageBaseName)
  - makeImageSourceResponse(sourceId, tier)

- getImageSourcePath(sourceId)
  - search globalPath.dataPaths for:
    - { type: "image_source", sourceId, path, label }
  - fallback:
    - old core_images path maps to source_1

- getTierDpcm(loadOptions, imageBaseName)
  - thumb -> thumb dpcm
  - standard -> normal dpcm
  - highres -> highres dpcm

- makeImageSourceResponse(sourceId, tier)
  - return object with metadata and tier image buckets

14. RegisterCoreImage handler flow
- Keep existing handler name.
- Extend payload contract to accept:
  - sourceId
  - label
  - type
- Store registration as:
  - { type: "image_source", sourceId, label, path }
- Backward compatibility:
  - if payload.type == "core_images" and no sourceId, map to source_1

15. Menu action currently labeled "Load Core Images"
- Keep minimal change:
  - still open a directory chooser
  - before registration, choose source slot or use current active source
- Send registration payload through existing RegisterCoreImage path

16. floatingImageViewer path and other explicit viewer loads
- When building loadOptions for viewer:
  - set sourceId = current active source
  - set tier = "highres"
- Include sourceId and tier in the message sent to ImageViewerMenuClicked

Preload: preload/preload.js

17. createCoreImageRegistrationPayload(input)
- Extend returned payload:
  - sourceId: input.sourceId ?? "source_1"
  - label: input.label ?? ""
- Keep type field for compatibility.

18. LoadCoreImage payload callers
- Wherever LoadCoreImage is called, support:
  - sourceId
  - tier

Preload viewer: preload/preload_image_viewer.js

19. createCoreImageRegistrationPayload(input)
- Mirror preload.js changes exactly.

20. LoadCoreImage payload callers
- Mirror preload.js changes exactly.

----------------------------------------------------------------
Recommended work order
----------------------------------------------------------------

Step 1. Data structure first
- File:
  - renderer/js/renderer.js
  - renderer/js/renderer_image_viewer.js
- Implement:
  - createImageTierBucket()
  - createImageSourceBucket()
  - ensureImageSource()
  - initialiseImages() rewrite
- Goal:
  - establish one consistent cache shape before touching load logic

Step 2. Main process source registration
- File:
  - main.js
  - preload/preload.js
  - preload/preload_image_viewer.js
- Implement:
  - sourceId + label payload support
  - image_source registration in globalPath.dataPaths
- Goal:
  - make image source selection addressable without changing the renderer again

Step 3. Main image load backend
- File:
  - main.js
- Implement:
  - getImageSourcePath()
  - getTierDpcm()
  - makeImageSourceResponse()
  - loadCoreImages(loadOptions, type) update
- Goal:
  - support source-aware and tier-aware loads while keeping one backend

Step 4. Renderer-side image assignment
- File:
  - renderer/js/renderer.js
  - renderer/js/renderer_image_viewer.js
- Implement:
  - assignCoreImages() rewrite
  - sourceId / tier aware receive handlers
- Goal:
  - image data lands in the correct cache bucket

Step 5. Standard-tier visible range loading
- File:
  - renderer/js/renderer.js
- Implement:
  - collectVisibleSectionIds()
  - refreshVisibleStandardImages()
- Goal:
  - stop loading full standard-resolution images for the full model

Step 6. Thumb preload
- File:
  - renderer/js/renderer.js
- Implement:
  - primeThumbImages()
- Trigger:
  - after model load
  - after source switch
- Goal:
  - immediate whole-model overview with low memory cost

Step 7. Drawing fallback order
- File:
  - renderer/js/renderer.js
- Implement:
  - getRenderableSectionImage()
- Fallback order:
  - standard -> thumb -> none
- Goal:
  - stable rendering during async replacement

Step 8. Explicit high-resolution path
- File:
  - renderer/js/renderer.js
  - main.js
  - renderer/js/renderer_image_viewer.js
- Implement:
  - loadHighresSectionImages()
  - viewer payload includes sourceId/tier
- Goal:
  - preserve current high-resolution behavior without bloating standard cache

Step 9. UI wiring
- File:
  - renderer/js/renderer.js
  - main.js if menu wiring is needed
- Implement:
  - active source selection
  - source registration entry point
- Goal:
  - make the new structure operable without redesigning the whole UI

----------------------------------------------------------------
Replacement / diff mapping table
----------------------------------------------------------------

1. Renderer image cache root

- Current:
  - function: initialiseImages()
  - returns:
    - image_dir
    - load_target_ids
    - image_resolution
    - plot_colour
    - drilling_depth
    - composite_depth
    - event_free_depth
    - age
    - operations

- New:
  - function: initialiseImages()
  - now returns:
    - source_meta
    - sources

- Added:
  - createImageTierBucket()
  - createImageSourceBucket(label = "")
  - ensureImageSource(modelImages, sourceId, label = "")

- Effect:
  - replaces one global image store with per-source tiered stores

2. Renderer settings

- Current:
  - function: setupSettings()
  - fields:
    - objOpts.canvas.is_core_photo_visible
    - objOpts.image.is_core_photo_visible
    - objOpts.image.dpcm
    - objOpts.image.dpcm_highresolution

- New:
  - function: setupSettings()
  - add:
    - objOpts.image.active_source_id
    - objOpts.image.thumb_dpcm
    - objOpts.image.standard_dpcm
    - objOpts.image.highres_dpcm
    - objOpts.image.visible_tier

- Effect:
  - extends current settings instead of replacing them

3. Renderer image load entry

- Current:
  - function: loadCoreImages(modelImages, LCCore, objOpts, operations)
  - implicit behavior:
    - full/global or changed-section image load
    - writes into flat modelImages buckets

- New:
  - same function name retained
  - new conceptual signature:
    - loadCoreImages(modelImages, LCCore, objOpts, operations, requestOptions = {})
  - requestOptions:
    - sourceId
    - tier
    - targetIds
    - label

- Added:
  - collectVisibleSectionIds(LCCore, objOpts)
  - primeThumbImages(modelImages, LCCore, objOpts)
  - refreshVisibleStandardImages(modelImages, LCCore, objOpts, operations)
  - loadHighresSectionImages(modelImages, LCCore, objOpts, targetIds, operations)

- Effect:
  - existing loader becomes a generic source/tier loader

4. Renderer image assignment

- Current:
  - function: assignCoreImages(coreImages, imageBuffers)
  - behavior:
    - decode all returned buffers
    - write to:
      - results[depthScale][imName]

- New:
  - same function name retained
  - behavior:
    - decode returned buffers
    - write to:
      - results.sources[sourceId][tier][depthScale][imName]

- Expected payload change:
  - sourceId
  - tier
  - data

- Effect:
  - assignment becomes source-aware and tier-aware

5. Renderer drawing lookup

- Current:
  - drawing code directly reaches into flat modelImages depth buckets
  - examples:
    - modelImages.drilling_depth[hole-section]

- New:
  - added helper:
    - getRenderableSectionImage(modelImages, objOpts, depthScale, sectionKey)
  - lookup order:
    - active source
    - standard if available
    - otherwise thumb
    - otherwise none

- Effect:
  - existing draw code keeps structure but no longer hardcodes a single source

6. Renderer current zoom behavior

- Current:
  - zoom affects display only
  - no explicit image tier decision

- New:
  - added helper:
    - getCurrentImageTier(objOpts)

- Effect:
  - zoom now decides whether main view uses thumb or standard

7. Renderer explicit high-resolution actions

- Current:
  - direct mutation:
    - modelImages.image_resolution[hole-section] = objOpts.image.dpcm_highresolution
  - followed by generic loadCoreImages(...)

- New:
  - replace those call sites with:
    - loadHighresSectionImages(modelImages, LCCore, objOpts, [targetId], operations)

- Effect:
  - separates highres cache from standard cache

8. Main process image backend

- Current:
  - function: loadCoreImages(loadOptions, type)
  - assumptions:
    - one type path list such as core_images
    - one dpcm policy
    - one returned image map layout

- New:
  - same function name retained
  - loadOptions additionally includes:
    - sourceId
    - tier
    - label

- Added:
  - getImageSourcePath(sourceId)
  - getTierDpcm(loadOptions, imageBaseName)
  - makeImageSourceResponse(sourceId, tier)

- Effect:
  - backend stays centralized but can load by source and tier

9. Main process image registration

- Current:
  - RegisterCoreImage payload:
    - dirHandle
    - type
  - stored path:
    - { type: "core_images", path }

- New:
  - RegisterCoreImage payload extended:
    - dirHandle
    - type
    - sourceId
    - label
  - stored path:
    - { type: "image_source", sourceId, label, path }

- Effect:
  - old core_images semantics are generalized into source slots

10. Main process menu action

- Current:
  - "Load Core Images"
  - registers one image directory and loads all targets

- New:
  - same menu can remain initially
  - before registration:
    - choose target source slot
  - after registration:
    - trigger thumb preload for active source

- Effect:
  - keeps UX small while allowing multiple sources

11. Viewer image cache

- Current:
  - renderer_image_viewer.js initialise()
  - flat cache:
    - drilling_depth
    - composite_depth
    - event_free_depth
    - age

- New:
  - same source-aware structure as renderer main

- Added:
  - createImageTierBucket()
  - createImageSourceBucket()
  - ensureImageSource()
  - getViewerPrimaryImage(modelImages, depthScale, sourceId, tier)

- Effect:
  - viewer and main share one mental model

12. Viewer image assignment

- Current:
  - assignCoreImages(coreImages, imageBuffers, objOpts)
  - writes into flat depth buckets

- New:
  - same function name retained
  - writes into:
    - modelImages.sources[sourceId][tier][depthScale][sectionKey]

- Effect:
  - viewer can display the same source chosen in the main view

13. Preload payload creation

- Current:
  - createCoreImageRegistrationPayload(input)
  - output:
    - dirHandle
    - type

- New:
  - same function name retained
  - output additionally includes:
    - sourceId
    - label

- Effect:
  - renderer can identify which source is being registered

14. Preload image load payloads

- Current:
  - LoadCoreImage(payload)
  - no source/tier semantics

- New:
  - same call retained
  - payload additionally carries:
    - sourceId
    - tier

- Effect:
  - same IPC entry point, richer semantics

----------------------------------------------------------------
E2E test additions and execution
----------------------------------------------------------------

Purpose
- This feature changes image loading behavior, caching, source switching, and tier selection.
- It must not be merged without E2E coverage for at least:
  - source registration
  - source switching
  - thumb-first rendering
  - visible-range standard loading
  - explicit highres loading path

New E2E test files to add

1. tests/e2e/image-source-switch.spec.js
- Goal:
  - verify multiple image sources can be registered
  - verify active source switching changes rendered source
- Main checks:
  - source_1 registration succeeds
  - source_2 registration succeeds
  - switching active source updates renderer state
  - rendered section image changes accordingly

2. tests/e2e/image-thumb-standard.spec.js
- Goal:
  - verify thumb images are loaded first for overview
  - verify standard images are requested only for visible sections
- Main checks:
  - after model load, thumb tier exists for active source
  - standard tier is absent or partial before view settles
  - after scroll / zoom into a subset, standard tier is loaded for visible sections only

3. tests/e2e/image-highres-viewer.spec.js
- Goal:
  - verify explicit viewer / high-resolution path loads only target section
- Main checks:
  - opening viewer for a section requests tier = highres
  - highres cache contains target section
  - non-target sections are not loaded into highres cache

Renderer E2E hooks to add

File:
- renderer/js/renderer.js

Add to window.__LC_E2E__.getRendererState()
- activeImageSourceId
- visibleImageTier
- imageSourceIds
- thumbLoadedSectionCount
- standardLoadedSectionCount
- highresLoadedSectionCount
- currentVisibleSectionIds

Add helper methods if needed
- setActiveImageSource(sourceId)
- forceRefreshVisibleImages()
- getLoadedSectionKeysByTier(tier)

Viewer E2E hooks to add

File:
- renderer/js/renderer_image_viewer.js

Add to window.__LC_VIEWER_E2E__.getState()
- activeImageSourceId
- viewerImageTier
- loadedHighresSectionCount
- loadedHighresSectionKeys

Main-process E2E observability to add

File:
- main.js

Add lightweight logs or counters exposed through existing E2E hooks for:
- last requested image sourceId
- last requested tier
- last requested targetIds count

This is needed to verify:
- thumb preload requests all sections
- standard requests only visible sections
- highres requests only explicit target ids

Recommended implementation order for tests

Step 10. Add renderer/viewer E2E hooks
- Do this before writing spec files.
- Without observability, assertions will be brittle.

Step 11. Add source-switch E2E
- File:
  - tests/e2e/image-source-switch.spec.js
- This is the first stable test because it does not depend on scroll timing as much.

Step 12. Add thumb/standard tier E2E
- File:
  - tests/e2e/image-thumb-standard.spec.js
- This verifies the main performance-sensitive behavior.

Step 13. Add highres viewer E2E
- File:
  - tests/e2e/image-highres-viewer.spec.js
- This verifies that highres is explicit and limited in scope.

Test execution requirements

During implementation, run at minimum:
- npm run test:e2e -- tests/e2e/image-source-switch.spec.js
- npm run test:e2e -- tests/e2e/image-thumb-standard.spec.js
- npm run test:e2e -- tests/e2e/image-highres-viewer.spec.js

Before completion, run:
- npm run test:e2e

Required acceptance criteria
- New image-source E2E tests pass
- Existing image-related E2E tests still pass
- Existing plot-related E2E tests still pass
- No test should depend on manual dialog interaction

Fixture guidance
- Use two clearly different fixture image sources
  - example:
    - source_1 = red-toned images
    - source_2 = blue-toned images
- Keep thumbnails visually distinct to make assertions simpler
- Keep section count small enough for deterministic E2E timing

----------------------------------------------------------------
Implementation status as of 2026-04-27
----------------------------------------------------------------

Implemented
- Source-aware image cache structure is implemented in renderer/js/renderer.js.
  - modelImages.sources[sourceId].thumb
  - modelImages.sources[sourceId].standard
  - modelImages.sources[sourceId].highres
  - modelImages.source_meta
- Active image source state is implemented.
  - objOpts.image.active_source_id
  - modelImages.active_source_id
- Tier state and resolution settings are implemented.
  - objOpts.image.visible_tier
  - objOpts.image.thumb_dpcm
  - objOpts.image.standard_dpcm
  - objOpts.image.highres_dpcm
- Source/tier helper functions are implemented in the main renderer.
  - createImageTierBucket()
  - createImageSourceBucket()
  - ensureImageSource()
  - syncLegacyImageAliases()
  - getImageTierBucket()
  - getCurrentImageTier()
  - getRenderableSectionImage()
- Standard image lazy loading is implemented.
  - collectVisibleSectionIds()
  - refreshVisibleStandardImages()
  - scheduleVisibleStandardImageRefresh()
  - standard loads use visible/buffer section ids instead of full-model standard loading.
- Thumb loading is implemented for all active-source sections.
  - Image import/drop loads thumb tier first.
  - Standard tier is refreshed separately for visible sections.
- Renderer-side image assignment is source/tier aware.
  - assignCoreImages() stores returned images under modelImages.sources[sourceId][tier][depthScale][sectionKey].
  - Legacy flat aliases are synchronized for compatibility.
- Main process image loading is source/tier aware.
  - loadCoreImages(loadOptions, type) accepts sourceId, tier, label, silentProgress.
  - getRegisteredImageSources(type, sourceId) selects registered paths by sourceId.
  - old core_images entries are treated as source_1 for compatibility.
- Main process image registration is source-aware.
  - RegisterCoreImage accepts sourceId and label.
  - RegisterCoreImageFromPath accepts sourceId and label.
  - registerCoreImage() stores source metadata.
- Floating image viewer high-resolution loading is source/tier aware.
  - viewer load path requests tier = highres.
  - sourceId is passed to the viewer-side load path.
- Viewer renderer has source-aware image buckets.
  - renderer/js/renderer_image_viewer.js uses source/tier structure.
  - viewer E2E state exposes activeImageSourceId and highres cache counts.
- Preload payloads carry source metadata.
  - preload/preload.js passes sourceId and label for image registration.
  - preload/preload_image_viewer.js mirrors the same payload shape.
- Cache limits and release behavior are implemented.
  - objOpts.image.standard_cache_limit
  - objOpts.image.highres_cache_limit
  - standard/highres tiers evict older entries.
  - standard eviction protects current visible/buffer section keys.
- Progress behavior was adjusted.
  - standard lazy loads can use silentProgress.
  - age image operations are excluded from the progress denominator when no age model is selected.
- E2E coverage was added and run.
  - tests/e2e/image-source-switch.spec.js
  - tests/e2e/image-thumb-standard.spec.js
  - tests/e2e/image-highres-viewer.spec.js
  - tests/e2e/helpers/app.js
  - Full E2E result before push: 41 passed.

Partially implemented
- Source switching exists as internal/E2E functionality and normal footer UI.
  - window.__LC_E2E__.setActiveImageSource(sourceId) works for registered sources.
  - ImageSetSelect provides a user-facing fixed-slot source selector.
- Multiple source registration is structurally supported.
  - sourceId and label are accepted by the IPC path.
  - The normal UI flow for creating/selecting source_2, source_3, etc. is not yet implemented.
- Menu naming and UX are still mostly legacy.
  - Existing "Load Core Images" style flow remains.
  - It is not yet generalized into a complete "Load Section Images / choose source slot" user flow.
- Main-view tier selection is conservative.
  - Main view uses standard with thumb fallback.
  - highres remains explicit viewer/request behavior, not automatic main-view rendering.

Not implemented yet
- User-facing source creation / naming flow.
  - Fixed ImageSet 1-5 slots are implemented.
  - Dynamic Add/Delete source slot management is not implemented and is not needed for the first GUI version.
- Source label editing.
  - Labels are stored, but there is no complete UI for editing them.
- Visual assertion that different sources produce different rendered pixels in normal UI.
  - Current E2E validates state/cache behavior, not full pixel-level visual difference.
- Main-process E2E counters for last requested sourceId/tier/targetIds.
  - Current tests rely mostly on renderer/viewer state.

Current practical behavior
- One active source is rendered at a time.
- Dropping/importing images registers them to the current active source.
- Thumb images are loaded broadly.
- Standard images are loaded for visible/buffer sections.
- High-resolution images are loaded only through explicit high-resolution/viewer paths.
- Switching to another source is supported by the ImageSet dropdown.
- Normal users can select one of three GUI-visible ImageSet slots.
- Internally, five ImageSet slots remain available.
- Dropped images are loaded into the currently selected ImageSet.

----------------------------------------------------------------
ImageSet GUI design proposal
----------------------------------------------------------------

Naming
- Use "ImageSet" as the user-facing term.
- Internal source ids can remain:
  - source_1
  - source_2
  - source_3
  - source_4
  - source_5
- User-facing labels should be:
  - ImageSet 1
  - ImageSet 2
  - ImageSet 3

Recommended placement
- Place the ImageSet dropdown in the footer control area.
- Put it before AgeModelSelect.
- Recommended order:
  - YAxisSelect
  - ImageSetSelect
  - AgeModelSelect
  - status text

Reason
- YAxisSelect chooses the vertical coordinate.
- ImageSetSelect chooses the image dataset rendered against that coordinate.
- AgeModelSelect is only relevant when age data or age axis is used.
- Therefore ImageSet should sit closer to YAxisSelect than AgeModelSelect.
- The top toolbar should remain mostly action/toggle tools.
- The existing bt_core_photo button should remain the image show/hide toggle.

Footer layout
- Current:
  - [YAxisSelect] [AgeModelSelect] [footerLeftText]
- Proposed:
  - [YAxisSelect] [ImageSetSelect] [AgeModelSelect] [footerLeftText]

Toolbar responsibility
- Keep:
  - bt_core_photo = image visibility on/off
- Do not use toolbar for source selection.
- ImageSet selection is state selection, not a transient tool action.

ImageSet dropdown behavior
- Internally keep five fixed slots.
- Show only three slots in the GUI for now.
- No Add button in the first implementation.
- No Delete button in the first implementation.
- No Rename UI in the first implementation.
- Initial selected value:
  - source_1 / ImageSet 1
- Options:
  - value: source_1, text: ImageSet 1
  - value: source_2, text: ImageSet 2
  - value: source_3, text: ImageSet 3

Drop behavior
- Dropped images are registered to the currently selected ImageSet.
- Example:
  - If ImageSet 3 is selected, dropped images are registered with sourceId = source_3.
- After drop:
  - register image path to selected sourceId
  - load thumb tier for that source
  - load standard tier for visible/buffer sections
  - updateView()
- If the selected ImageSet has no registered image path, it renders no images.

Source switch behavior
- On ImageSetSelect change:
  - update objOpts.image.active_source_id
  - sync legacy image aliases
  - refresh visible standard images for the selected source if needed
  - updateView()
- Do not reload all standard images on switch.
- Thumb can be loaded broadly for the selected source if it has a registered path and thumb cache is empty.

Status text
- Do not show a persistent "Drop images into ImageSet N" hint in the footer.
- The selected ImageSet dropdown value is the drop target indicator.

Implementation target
- File:
  - renderer/index.html
- Add:
  - select id="ImageSetSelect"
- Location:
  - between YAxisSelect and AgeModelSelect

- File:
  - renderer/js/renderer.js
- Add:
  - initialiseImageSetSelect()
  - handleImageSetChange(event)
- Wire:
  - ImageSetSelect change event
  - handleCoreImageImport() already uses objOpts.image.active_source_id, so drop imports should follow the selected ImageSet once this state is updated.

Acceptance criteria
- ImageSet dropdown appears between YAxisSelect and AgeModelSelect. Implemented.
- Three GUI-visible ImageSet options are available. Implemented.
- Five internal ImageSet slots remain initialized. Implemented.
- Selecting ImageSet changes objOpts.image.active_source_id. Implemented.
- Dropping images loads them into the selected ImageSet. Implemented through existing drop/import path.
- Switching back to another ImageSet restores its cached images if available. Implemented by source-aware cache and alias sync.
- Existing bt_core_photo behavior remains unchanged. Implemented.
- No LC_modules changes. Confirmed.
